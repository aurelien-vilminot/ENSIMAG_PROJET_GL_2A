class MaClasse {
    /*
    Attributs
     */
    int attributEntier = 1.3; // 4) Un entier ne peut pas avoir une valeur flottante
    protected float attributFlottant = 3;
    string attributChaine = "Hello World"; // 3) Une variable ne peut avoir le type "string"

    /*
    Méthodes
     */
    int maMethode(int monParametre, ) { // 1) Virgule en trop
        if (monParametre != 5) {
            return (5 / monParametre);
        } // 8) L'instruction "return" est manquante dans un cas !
    }
}

{
    /*
    Déclarations
     */
    MaClasse maClasseNull = null;
    MaClasse maVariableDeClasse = new MaClasse();

    /*
    Instructions
     */
    maVariableDeClasse.maMethode(5, 0); // 5) Trop de paramètres
    maVariableDeClasse.maMethode(0); // 6) Division par zéro
    println(maClasseNull.maMethode(4)); // 7) Déréférencement de null
    println("Hello World !");
/*
2) Oubli d'accolade fermante
 */
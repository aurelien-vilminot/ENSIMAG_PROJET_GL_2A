#include "Library.decah"

{
    AlgebraLib Mat = new AlgebraLib();

    float[][] matrix = Mat.uniformMatrix(2, 2, -3.0), matrix2 = Mat.uniformMatrix(2, 2, 5.0);
    float[][] matrix5 = matrix = Mat.addMat(matrix, matrix2);

    Mat.printMatrix(matrix5);
}